# Homework: Functions, Strings, Arrays & Objects

## 🧩 Level 1: Basic Practice

These will help students recall syntax and fundamentals.

### 🧠 Functions

1. **Create a function `greetUser(name)`** that prints:

   ```
   "Hello [name], welcome to the bootcamp!"
   ```

2. **Write a function `squareNumber(num)`** that returns the square of a given number.

3. **Create a function `isAdult(age)`** that returns:

   - `true` if age ≥ 18
   - `false` otherwise

4. **Write a function `calculateArea(radius)`** to find the area of a circle using πr².

---

### 🧠 Strings

1. **Take a string:** `" JavaScript is Awesome "`

   - Print the string's length.
   - Convert it to uppercase.
   - Trim spaces.
   - Replace "Awesome" with "Powerful".

2. **Create a string template:**

   ```javascript
   const name = "Ajay";
   const age = 25;
   ```

   **Output:**

   ```
   "Hi, I'm Ajay and I am 25 years old."
   ```

---

## 🧩 Level 2: Intermediate Practice

Now we move to arrays and higher-order functions.

### 🧠 Arrays

1. **Create an array of your 5 favorite fruits.**

   - Add 1 new fruit to the beginning and end.
   - Remove the first and last fruit.
   - Print final array.

2. **Given this array:**

   ```javascript
   const marks = [85, 42, 97, 63, 77, 56];
   ```

   - Use `.filter()` to get marks greater than 60.
   - Use `.map()` to add 5 bonus marks to each.
   - Use `.reduce()` to find the total marks.

3. **Write a function `findMax(arr)`** that returns the largest number in an array (without using `Math.max`).

---

### 🧠 Strings + Arrays Combo

1. **Take a string** `"hello my name is imran"`.
   - Convert it into an array of words and then reverse the words to form `"imran is name my hello"`.
